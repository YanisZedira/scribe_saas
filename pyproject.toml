[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = ["B008"] # FastAPI injecte ses dépendances dans les valeurs par défaut.

[tool.pytest.ini_options]
testpaths = ["server/tests"]
